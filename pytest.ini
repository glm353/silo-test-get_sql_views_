[pytest]
# Use a repo-local tmp dir — avoids a Windows/OneDrive PermissionError when pytest cleans up the
# "pytest-current" symlink under the system temp on exit.
addopts = --basetemp=.pytmp
testpaths = tests
